
BUILD files are based on the Makefile.am files in each directory, which builds with the legacy non-DOM libraries, which is why only certain files are included in the build.
Only supports Linux and Mac for now, can add more platforms as needed.
